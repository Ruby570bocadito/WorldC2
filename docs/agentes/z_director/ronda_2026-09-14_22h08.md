# Informe de ronda — AGENTE DIRECTOR

- **Fecha y hora de la ronda:** 2026-09-14 22h08 (Europe/Madrid)
- **Agente:** z_director
- **Repositorio:** Ruby570bocadito/WorldC2 @ `main` (HEAD `d9f12c9`, ronda 1 publicada)
- **Ronda:** 2

---

## Resumen ejecutivo

Segunda ronda de auditoría. El trabajo previo a la ronda consistió en verificar el estado real del repositorio contra los cuatro informes de la ronda 1: baseline ejecutado de nuevo (`go build`, `go vet`, `go test -race ./...` — todo verde, 7 paquetes con tests) y verificación en vivo de tres de las correcciones de la ronda 1 (persistencia de comandos, revocación JWT con su suite de tests, rate limit de login). El foco de esta ronda era el riesgo R1 heredado — el transporte HTTP long-poll documentado como experimental y sin capacidad real de conectar agentes — que ha quedado **reparado de extremo a extremo y verificado con una prueba de humo en vivo**: un agente real ha establecido sesión a través del túnel y ha ejecutado un comando con su resultado de vuelta. La auditoría también ha destapado **dos bugs nuevos** (inconsistencia de `TaskCount` en el detalle de sesión y roles de operador sin validar en la creación) que z_bugs ha corregido en esta misma ronda. El estado general pasa de AMARILLO a **VERDE**, con deuda técnica conocida y documentada.

---

## Estado general del proyecto: 🟢 VERDE

**Justificación:** el núcleo compila, pasa `go vet` limpio y la batería `-race` completa está en verde; los cinco transportes declarados en el README son ahora reales y verificables (cuatro con tests de loopback, el DNS con tests de roundtrip propios); la superficie API coincide con la especificación; los fixes de la ronda 1 se han comprobado operativos en un servidor real. Quedan riesgos abiertos de severidad media (anti-replay, reaper de túneles, KDF del cifrado at-rest, tokens en `localStorage`), pero todos están **documentados como backlog explícito** en los informes de agente y ninguno compromete la operación normal en laboratorio autorizado. La honestidad documental del README vuelve a estar completa: ya no queda ninguna feature marcada ✅ que no funcione.

---

## Verificación del trabajo de la ronda 1 (heredado)

- **Implementaciones (R3, R4, R8):** el flag `-no-persist` sigue presente y documentado; `auth/jwt_test.go` cubre revocación, pinning de algoritmo y separación access/refresh (9 tests, verdes); el limiter de login se ha re-verificado en vivo en esta ronda tras tocar la resolución de IP (10 × 200 y después 429 — comportamiento exacto).
- **Pulimiento (R9, R10):** sin marcadores `var _ =` regresivos (los nuevos ficheros creados en esta ronda no introducen ninguno); ANSI de `tests/` intacto; `.gitignore` sigue cubriendo `reports/` y `dist/`.
- **Bugs (R2, R5, R6, R7):** `CreateTaskWithContext` persiste y audita (verificado en vivo: el comando del smoke aparece en `/api/sessions/{id}` con su output); los atómicos y backoffs de portfwd/socks están en su sitio; los 9 handlers devuelven 400 ante JSON inválido; la admisión de sesiones sigue el patrón check-antes-de-persistir.

---

## Riesgos detectados en esta ronda

| # | Prioridad | Riesgo | Detalle | Resolución |
|---|-----------|--------|---------|------------|
| N1 | **ALTA** (heredado R1) | Transporte HTTP long-poll inservible | Confirmado en la auditoría de código: el fallback "HTTP" del agente era un `net.DialTimeout` TCP crudo (400 garantizado contra un handler HTTP), `GetPendingWrite` seguía sin una sola llamada en el repo, `httpConn.Read` bloqueaba reteniendo el mutex que `Write` necesita y descartaba el excedente del framing; además el handler usaba el mismo canal para cola de entrada y respuesta (auto-consumo). | **Corregido por z_implementaciones en esta ronda** (reescritura completa + 7 tests de loopback + prueba de humo en vivo). |
| N2 | MEDIA | Rate limiter ciego a proxies inversos | Tras nginx/traefik todas las peticiones comparten la IP del proxy: un solo cubo para todos los operadores (DoS accidental) y el límite de login deja de blunter fuerza bruta por víctima. | **Corregido por z_implementaciones** (config `server.trusted_proxies` + resolución estándar de `X-Forwarded-For` con tests). |
| N3 | MEDIA-BAJA | `GetSession` sin subquery `task_count` | `GET /api/sessions/{id}` reportaba `TaskCount: 0` mientras su propia lista de tasks mostraba N tareas (detectado en el smoke en vivo de esta ronda). | **Corregido por z_bugs.** |
| N4 | MEDIA-BAJA | Roles de operador sin validar en la creación | Un operador del config sin `role:` (o con typo) se creaba, autenticaba y después fallaba en cada endpoint RBAC con un confuso `authentication required`. Verificado en vivo durante el smoke de esta ronda. | **Corregido por z_bugs** (validación en 3 superficies: API, seed del config y refresh). |
| N5 | BAJA | Puertos de transporte hardcodeados en el agente | El agente asume 8445/8446/8447 por convención; un despliegue con puertos no estándar necesita reglas extra. No es bug (está documentado por convención) — anotado como backlog de UX. | Backlog. |
| N6 | BAJA | Salud del transporte long-poll sin reaper propio | Resuelto dentro de N1 con el idle-guard de 10 min (los heartbeats llegan cada 25-35 s); el reaper de **túneles** y transfers de exfil sigue siendo backlog. | Parcialmente resuelto. |

Riesgos **no** bloqueantes arrastrados del backlog de la ronda 1 (sin cambios): anti-replay por números de secuencia, loot listing en BD (`file_records` sin usar), KDF del cifrado at-rest sin estiramiento, tokens en `localStorage`, `/api/health` público con telemetría, rotación de refresh tokens con `jti`.

---

## Decisiones tomadas por ti en esta ronda

1. **Prioridad única para Implementaciones: R1.** El transporte HTTP era el único riesgo ALTA del backlog y se decidió repararlo con diseño propio (dos endpoints explícitos, colas FIFO por dirección) en lugar de parchear el protocolo roto, acompañado del test de loopback que la ronda 1 exigió como condición para tocarlo.
2. **Decisión de producto sobre `task_queue`:** la capa muerta de cola offline se **poda** (código) conservando la migración 7 en el ledger (integridad histórica de BDs existentes). Si en el futuro se quiere cola offline, se rediseñará sobre la tabla `tasks` real, no sobre este código sin usar.
3. **Alcance de la ronda:** se descartaron para esta ronda la persistencia de webhooks en BD, el loot listing en BD y la migración KDF — cada uno exige migración o decisión de flujo de sesión; permanecen en backlog priorizado.
4. **Baseline de regresión re-registrado:** build ✅, vet ✅, gofmt limpio, `go test -race -count=1 ./...` verde (7 paquetes). Todo cambio de esta ronda mantiene el baseline; los tests nuevos (7 de transporte + 5 de rate limit) lo refuerzan.
5. **Verificación en vivo obligatoria:** cualquier fix que afecte al flujo de comandos o de autenticación debe demostrarse contra un servidor real antes del commit (hecho para N1, N3, N4 y el limiter de login).
